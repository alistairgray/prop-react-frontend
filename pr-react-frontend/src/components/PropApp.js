import React, {useState} from 'react';
import Header from './Header';

function PropApp() {
    // Declare State
    const [] = useState(0);

    return (
        <div>
            <Nav />
            <Header />
        </div>
    );
}

